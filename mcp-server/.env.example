# ========================================
# MCP SERVER - VARIABILI AMBIENTE
# ========================================
# Copia questo file in .env e compila con i tuoi valori

# MongoDB Atlas (STESSO del backend)
MONGODB_URI=mongodb+srv://pastificio:YOUR_PASSWORD@cluster0.mongodb.net/pastificio?retryWrites=true&w=majority

# JWT Secret (STESSO del backend - per compatibilità futura)
JWT_SECRET=YOUR_JWT_SECRET

# Backend API URL (se necessario per chiamate HTTP)
BACKEND_URL=https://pastificio-completo-production.up.railway.app

# Node Environment
NODE_ENV=production

# Logging
LOG_LEVEL=info

# ========================================
# ISTRUZIONI:
# ========================================
# 1. Copia questo file: cp .env.example .env
# 2. Compila MONGODB_URI con la password reale
# 3. Compila JWT_SECRET (se presente nel backend)
# 4. Salva e NON fare commit di .env (è già in .gitignore)
#
# NOTA: Le credenziali devono essere IDENTICHE 
# a quelle del backend per accedere allo stesso database.
# ========================================
