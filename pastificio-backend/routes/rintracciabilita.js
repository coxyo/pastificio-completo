// routes/rintracciabilita.js
// Placeholder - Route rintracciabilità HACCP
import express from 'express';
const router = express.Router();

// TODO: implementare route rintracciabilità
router.get('/', (req, res) => {
  res.json({ success: true, data: [], message: 'Rintracciabilità - da implementare' });
});

export default router;